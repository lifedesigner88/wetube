extends base
block content
    form(method ="POST")
        input(name="title", type="text", placeholder="Title", maxlength=80, required )
        input(name="description", type="text", placeholder="Description", required , minlength=20)
        input(name="hashtags", type="text", placeholder="Hashtags, seprated by , ", required )
        input(type="submit", value="Upload Video")